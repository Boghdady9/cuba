apiVersion: v1
kind: Service
metadata:
  name: cuba-api
  namespace: cuba
  labels:
    app.kubernetes.io/name: cuba-api
spec:
  selector:
    app: cuba-api
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
